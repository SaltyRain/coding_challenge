import { Fragment, ReactElement } from "react";
import { Card, Typography,  } from '@mui/material';

export default function StartupListItem(): ReactElement {
  return <Fragment>
    <Card>
      <Typography variant={'h3'} component={'h3'}></Typography>
      
    </Card>
  </Fragment>;
}